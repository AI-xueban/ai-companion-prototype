{
  "name": "wanxiang-prototype",
  "version": "1.0.0",
  "private": true,
  "description": "万象视界管理后台原型",
  "scripts": {
    "dev": "serve prototype -l tcp://0.0.0.0:8080",
    "start": "npm run dev",
    "build": "node scripts/build.js",
    "preview": "serve dist -l 8080",
    "package": "npm run build && node scripts/package.js"
  },
  "devDependencies": {
    "serve": "^14.2.4"
  }
}
