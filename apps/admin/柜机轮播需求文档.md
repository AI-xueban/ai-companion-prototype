# AI学伴管理后台 — 柜机轮播需求文档

> **【历史归档】** 本文档已并入 [设备管理需求文档.md](./设备管理需求文档.md) §4.16 与 §5.6.4～§5.6.6，自 2026-08-27 起不再单独维护。新增/修改请直接在《设备管理需求文档》中操作，本文档仅作历史版本留档，内容可能与最新版本不一致。

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 柜机轮播需求文档 |
| 文档版本 | v2.1 |
| 创建日期 | 2026-08-04 |
| 修订日期 | 2026-08-05 |
| 文档状态 | 历史归档（已并入设备管理需求文档，不再单独维护） |
| 所属项目 | AI学伴管理后台 · 设备管理 · 柜机管理 |
| 关联文档 | [设备管理需求文档.md](./设备管理需求文档.md)；[万象视界需求文档.md](./万象视界需求文档.md) |
| 原型入口 | `prototype/index.html` → 设备管理 → **柜机管理** |
| 数据持久化 | `localStorage` 键名 `ai_study_prototype_v44`（与万象视界 / 设备管理共用） |

---

## 2. 范围

为借租**柜机广告区**下发图片轮播素材：按柜独立配置，或批量覆盖多台。能力融入**柜机管理**，无独立一级菜单。

不在本文范围：学生端首页热门轮播（万象视界「热门推荐」）；柜机端播放器实现细节。

```
设备管理
└── 柜机管理（cabinet）
    ├── 工具栏：批量配置轮播内容
    ├── 列表：轮播素材、查看轮播、配置轮播
    ├── 配置轮播（cabinet-carousel）
    └── 批量配置轮播（cabinet-carousel-batch）
```

---

## 3. 目标

1. 不同柜机可下发不同广告图片。
2. 支持单柜配置与多柜批量覆盖。
3. 配置前可在后台按广告区比例预览后再下发。

---

## 4. 业务规则

| 项 | 规则 |
|----|------|
| 广告区尺寸 | **1080×600** |
| 素材类型 | **图片**（本地上传：jpg / png / webp / gif） |
| 单柜上限 | **3 条**；满额时「+ 添加图片」置灰，旁注「已达上限」 |
| 播放顺序 | 列表排序；支持上移 / 下移 |
| 图片停留 | 系统默认 **5 秒**（全柜统一，后台不提供配置入口） |
| 单柜保存 | 「保存并下发」写回该柜配置 |
| 批量保存 | 默认空列表；保存后**覆盖**所选柜机原有全部轮播（空列表即清空） |
| 删除单条 | 配置页删除后需再保存才同步 |

**数据（逻辑）：**

```
cabinetCarousels[cabinetId] = {
  items: [ { id, type: 'image', name, url, sort } ],
  updatedAt
}
```

`items` 为空或不存在：该柜无轮播内容。

---

## 5. 页面规格

### 5.1 柜机管理列表（`cabinet`）

工具栏：添加柜机、**批量配置轮播内容**；右侧显示已选台数。未勾选柜机点批量时 Toast 提示。

列表与轮播相关列：勾选、**轮播素材**（缩略图，超出显示 +N；无素材为「暂无素材」）、操作含**查看轮播** / **配置轮播**。

筛选项可含柜机名称、在线状态、安装位置、广告轮播状态等（原型占位）。

### 5.2 查看轮播弹窗

入口：列表「查看轮播」。按 1080×600 比例预览；多则可切换；下方表列为序号、素材名称。底部：关闭 / 去配置。

### 5.3 单柜配置（`cabinet-carousel`）

| 区块 | 说明 |
|------|------|
| 标题 | 配置轮播 · {柜机名称} |
| 柜机端预览 | 广告区比例预览当前素材；多则可切换；无角标 |
| 轮播素材列表 | 标题「轮播素材列表（上限3条）」 |
| 列表列 | 排序、素材名称、操作（删除） |
| 底部 | 取消 / 清空全部（有素材时）/ 保存并下发 |

添加图片：本地上传（点击或拖拽），建议尺寸 1080×600，上传后可预览再确认添加。

### 5.4 批量配置（`cabinet-carousel-batch`）

勾选 ≥1 台后进入。标题「批量配置轮播 · 已选 N 台」；说明例：`已选 N 台：柜机A、柜机B。保存后将覆盖其原有轮播内容。`  
默认空列表；空态文案「暂无素材，请添加图片」。编辑能力同单柜。「保存并批量下发」需二次确认。

---

## 6. 路由

| 路由 | 页面 |
|------|------|
| `#cabinet` | 柜机管理列表 |
| `#cabinet-carousel?{"id":"CAB-xxx"}` | 单柜配置轮播 |
| `#cabinet-carousel-batch?{"ids":["CAB-001","CAB-002"]}` | 批量配置轮播 |

---

## 7. 验收要点

- [ ] 轮播入口在柜机管理；无独立一级菜单
- [ ] 列表可查看 / 配置轮播；含轮播素材缩略列与批量配置
- [ ] 单柜 / 批量均可添加图片（本地上传）、调序、删除；上限 3，满额置灰并提示已达上限
- [ ] 批量默认空，保存覆盖所选柜机原内容
- [ ] 预览区比例 1080×600；无类型/时长角标；素材表无预览/类型/时长列
- [ ] 柜机详情页无轮播区块

---

## 8. 修订记录

| 版本 | 日期 | 说明 |
|------|------|------|
| v2.1 | 2026-08-06 | 明确本期仅图片轮播；移除 mock 视频素材与列表 ▶ 角标 |
| v2.0 | 2026-08-05 | 按当前原型重写：仅保留现有能力；本地上传图片；精简表述 |
| v1.0 | 2026-08-04 | 首版 |
